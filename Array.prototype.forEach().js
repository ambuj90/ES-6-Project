const array1 = ['a', 'b', 'c'];

array1.forEach(element => console.log(element));

// expected output: "a"
// expected output: "b"
// expected output: "c"


 var myFriends = ['ambuj','shivani','rohit','madhulika'];

 myFriends.forEach(function(element, index, array) {
       console.log(element   + " : " + 
         index  );
});
